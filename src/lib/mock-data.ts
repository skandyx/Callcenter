import { type CallData } from "@/lib/types";

// This file is intentionally empty to rely on real-time data pushed to the /api/stream endpoint.
// Renaming to call-data-store.ts in next step.
export const callDataStore: CallData[] = [];
